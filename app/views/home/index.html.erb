<h1>Home#index</h1>
<p>Login as <%= current_user ? current_user.email : '-'  %></p>
<p>is_admin:  <%= current_user ? current_user.is_admin : 'cant access'  %> </p>
<p>is_organization: <%= current_user ? current_user.is_organization : 'cant access'  %> </p>
<p>is_family: <%= current_user ? current_user.is_family : 'cant access'  %> </p>
